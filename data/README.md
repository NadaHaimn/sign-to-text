Project data folder

Place your trained model weights and related files here.

Expected files (examples):
- `best_arabic_sign_model.keras` or `final_arabic_sign_model.keras`
- `label_map_arabic.json`

Keep large model files in this folder so the project code loads them from `data/`.
